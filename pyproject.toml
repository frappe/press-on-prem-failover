[project]
name = "press-on-prem-failover"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "rq>=2.7.0",
]

[tool.uv.workspace]
members = [
    "press-on-prem-failover",
]
